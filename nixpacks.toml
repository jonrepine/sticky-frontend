# Nixpacks configuration for Railway deployment
# This builds a static frontend bundle and uses vite preview to serve it

[variables]
NPM_CONFIG_PRODUCTION = "false"

[phases.install]
cmds = [
  "npm install --include=dev"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm run start"
